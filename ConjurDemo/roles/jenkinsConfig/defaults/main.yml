---
# defaults file for jenkinsConfig

conjur_version: 'EE'

jenkins_url: "http://{{ ansible_default_ipv4.address }}:8080"
jenkins_image_name: 'conjur_jenkins'
jenkins_container_name: 'jenkins'

job1_config: "{{ lookup('file','{{ role_path}}/files/LAB1_Summon/config.xml') }}"